import { SortByGoalPipe } from './sort-by-goal.pipe';

describe('SortByGoalPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByGoalPipe();
    expect(pipe).toBeTruthy();
  });
});
